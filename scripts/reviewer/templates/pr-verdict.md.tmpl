<!--
  Reviewer PR-review body template (PET-135). Claude fills the {{...}} placeholders and
  posts this as a `gh pr review` body (--approve when VERDICT=approve, --request-changes
  when VERDICT=changes). Keep it self-contained: Pedro and the worker read THIS, not the
  reviewer's chain of thought. Drop the HTML comment when posting.
-->
## 🔬 Reviewer verdict — {{VERDICT_EMOJI}} {{VERDICT_UPPER}}

**Independent `bun test`:** {{TESTS_RESULT}}  <!-- pass ✅ / fail ❌ — re-run by the reviewer, not the worker's claim -->
**Head reviewed:** `{{HEAD_SHA}}`
**Linear:** {{ISSUE}}

### Findings
{{FINDINGS}}
<!-- numbered, specific, file:line where possible; or "None — no blocking issues found." -->

### Merge recommendation
{{ONE_LINE_RECOMMENDATION}}
<!-- one line, e.g. "Safe to merge once tests pass." / "Request changes: <reason>." -->

---
<sub>🤖 Automated review by the reviewer loop on agent-loop-242 (PET-135). Not a merge — Pedro merges. Max 2 round-trips, then `needs-human`.</sub>
