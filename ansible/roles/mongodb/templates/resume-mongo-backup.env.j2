# {{ ansible_managed }}
# Root-only (0600) — resolved once at playbook-run time from Vault kv/services/resume-builder.
# See defaults/main.yml for the no-committed-secret rationale.
MC_ACCESS_KEY={{ mongodb_backup_minio_access_key }}
MC_SECRET_KEY={{ mongodb_backup_minio_secret_key }}
MONGODB_APP_PASSWORD={{ mongodb_app_password }}
