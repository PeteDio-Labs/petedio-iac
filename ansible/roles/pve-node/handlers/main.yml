---
- name: Reload networking
  ansible.builtin.command: ifreload -a
  changed_when: true

- name: Restart logind
  ansible.builtin.systemd:
    name: systemd-logind
    state: restarted
    daemon_reload: true
