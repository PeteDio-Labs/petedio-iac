---
# agent-loop handlers (PET-141). Restart the Vault Agent when its config or unit changes.
- name: Restart vault-agent
  ansible.builtin.systemd_service:
    name: vault-agent
    state: restarted
    daemon_reload: true
