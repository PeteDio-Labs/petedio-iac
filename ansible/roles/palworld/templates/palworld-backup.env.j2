# {{ ansible_managed }}
# Root-only (0600) — resolved once at playbook-run time from Vault
# kv/services/palworld-backups. See defaults/main.yml for why this is not a live Vault
# read (mirrors resume-mongo-backup.env.j2, not vault-snapshot's AppRole login).
MC_ACCESS_KEY={{ palworld_backup_minio_access_key }}
MC_SECRET_KEY={{ palworld_backup_minio_secret_key }}
